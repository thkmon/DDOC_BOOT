version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/app/step2/zip/
    overwrite: yes

# CodeDeploy 에서 EC2 서버로 넘겨준 파일들 모두 ec2-user 권한을 갖도록 함
#permissions:
#  - object: /
#    pattern: "**"
#    owner: ec2-user
#    group: ec2-user
#
hooks:
  ApplicationStart:
    - location: deploy.sh
      timeout: 60
      runas: ec-user